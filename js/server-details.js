    // Boolean to determine the environment
    const isProduction = false; // Set to true for production, false for development

    // Set serverName and portNumber based on the environment
    // const serverName = isProduction ? 'https://crypto-invest-server.onrender.com' : 'http://localhost';
    // const portNumber = isProduction ? '' : 3000; // Leave portNumber empty for production
    const portNumber = 3000;
    const baseUrl = 'https://crypto-invest-server.onrender.com';
    const localBaseUrl = `http://localhost:${3000}`;
